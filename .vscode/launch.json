{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Debug Test", // Ваша назва може бути іншою
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/x64/Debug/test_repos.exe", // <-- ВАЖЛИВО: Перевірте цей шлях!
            "args": [
                "--gtest_output=xml:test_report.xml"
            ],
            "cwd": "${workspaceFolder}", // Вказує, де буде створено звіт
            "stopAtEntry": false,
            "environment": [],
            "console": "internalConsole"
        }
        // ...можуть бути інші конфігурації...
    ]
}